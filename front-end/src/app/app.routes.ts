import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Asegúrate de usar la ruta correcta

export const routes: Routes = [
  { path: '', component: AppComponent } // Ruta por defecto
];